import {Link} from 'react-router-dom';

function SupportList() {
    return (
        <div>
            Support List
            <Link to="/">
                <img src="fist.png"/>
            </Link>
        </div>
    );
};

export default SupportList;